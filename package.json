{
  "name": "image-grid",
  "version": "1.0.0",
  "description": "Display a grid over an image",
  "author": "Tarrask",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "DEPLAY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "bootstrap-vue": "^2.0.0-rc.11",
    "cross-env": "^5.2.0",
    "nuxt": "^2.4.0",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
